// @flow

import type {Theme} from '../types';

const dark: Theme = {
  name: 'dark',
};
const normal: Theme = {
  name: 'normal',
};
const contrast: Theme = {
  name: 'contrast',
};

export default [dark, normal, contrast];
